[package]
name = "esquery-json"
version = "0.1.0"
edition = "2024"
description = "ESQuery matcher for JSON ESTree ASTs"
license = "MIT"

[dependencies]
esquery-selector = { path = "../esquery-selector" }
serde_json = "1.0.150"
fancy-regex = "0.18.0"
