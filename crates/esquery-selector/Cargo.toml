[package]
name = "esquery-selector"
version = "0.1.0"
edition = "2024"
description = "ESQuery selector parser for JavaScript/TypeScript AST querying"
license = "MIT"

[dependencies]
winnow = "0.7.14"
